<!DOCTYPE html>
<html class="fontawesome-i2svg-active fontawesome-i2svg-complete">
  <head>
    <title>Bookers2App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
          <a class="navbar-brand" href="/">
            <span>Bookers</span>
          </a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
          aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="navbar-collapse collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
            <% if user_signed_in? %>

              <%= link_to user_path(current_user), class:'text-decolation-none' do %>
                <li class="nav-link text-light"><i class= "fas fa-home fa-w-18"></i> Home</li>
              <% end %>

              <%= link_to users_path, class:'text-decolation-none' do %>
                <li class="nav-link text-light"><i class="fas fa-users fa-w-20"></i> Users</li>
              <% end %>

              <%= link_to books_path, class:'text-decolation-none' do %>
                <li class="nav-link text-light"><i class="fas fa-book-open fa-w-18"></i> Books</li>
              <% end %>

              <%= link_to destroy_user_session_path, method: :delete, class:'text-decolation-none' do %>
                <li class="nav-link text-light"><i class="fas fa-sign-out-alt fa-w-16"></i> Log out</li>
              <% end %>

            <% else %>
              <%= link_to root_path, class:'text-decolation-none' do %>
                <li class="nav-link text-light"><i class= "fas fa-home fa-w-18"></i> Home</li>
              <% end %>

              <%= link_to about_path, class:'text-decolation-none' do %>
                <li class="nav-link text-light"><i class="fas fa-link fa-w-16"></i> About</li>
              <% end %>

              <%= link_to new_user_registration_path, class:'text-decolation-none' do %>
                <li class="nav-link text-light"><i class="fas fa-user-plus fa-w-20"></i> Sign up</li>
              <% end %>

              <%= link_to new_user_session_path, class:'text-decolation-none' do %>
                <li class="nav-link text-light"><i class="fas fa-sign-in-alt fa-w-16"></i> Log in</li>
              <% end %>

            <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>

    <%= render 'layouts/flash' %>
    <%= yield %>

    <footer>
      <div class ="container mt-5">
        <div class ="row">
          <div class ="mx-auto">
            <p>Copyright Infratop.inc</p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
